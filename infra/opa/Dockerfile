# OPA com policies Rego embutidas na imagem (sem bind mount externo).
# Trocar policy = rebuild da imagem (rm bind = robustez no deploy).
FROM openpolicyagent/opa:1.0.0-static
COPY policies /policies
