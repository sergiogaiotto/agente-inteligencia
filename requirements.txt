# ══════════════════════════════════════════
# Maestro — Dependências
# Instalar: pip install -r requirements.txt
#
# ⚠️ Versões PINADAS (==) para builds REPRODUZÍVEIS. Refletem o conjunto
# resolvido e validado em runtime (app healthy) em 2026-07-09. O Dependabot
# continua propondo bumps destes pins via PR (revisáveis + CI) — a diferença é
# que agora o build usa exatamente a versão testada, não "a mais nova que casa".
# ══════════════════════════════════════════

# === Core ===
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-dotenv==1.2.2
python-multipart==0.0.32
jinja2==3.1.6
aiofiles==25.1.0

# === Database (PostgreSQL) ===
asyncpg==0.31.0
# Alembic — migrações versionadas (up/down) adotadas na Onda 4 (33.6.0). O app
# runtime segue usando asyncpg cru; alembic usa seu próprio engine sync (psycopg2)
# só p/ aplicar migrações no boot (init_db, em thread). Traz SQLAlchemy como dep.
alembic==1.13.3
psycopg2-binary==2.9.12

# === Cache / Memória de Contexto ===
redis[hiredis]==8.0.1

# === LangChain / LangGraph ===
langgraph==1.2.9
langchain==1.3.2
langchain-openai==1.3.4
langchain-community==0.4.2
langchain-core==1.4.9

# === LLM Providers ===
openai==2.45.0
httpx==0.28.1

# === Observabilidade ===
langfuse==4.14.0

# === Observabilidade self-hosted (Onda 2 — OTel + Tempo + Loki + Grafana) ===
# Habilitada em runtime apenas quando OTEL_ENABLED=true. Caso contrário,
# init_otel() retorna no-op e nada destas libs é exercitado.
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-grpc==1.43.0
opentelemetry-instrumentation-fastapi==0.64b0
opentelemetry-instrumentation-asyncpg==0.64b0
opentelemetry-instrumentation-httpx==0.64b0
opentelemetry-instrumentation-redis==0.65b0
opentelemetry-instrumentation-logging==0.64b0
# Métricas de 1ª classe (OBS-1) — /metrics em formato Prometheus. Sempre ativo
# (não gated por OTEL_ENABLED): é in-memory e barato, exposto p/ scrape do Prometheus.
prometheus-client==0.21.1

# === Utilities ===
pydantic==2.13.4
pydantic-settings==2.14.2
pyyaml==6.0.3
websockets==15.0.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1   # passlib 1.7.4 quebra com bcrypt 4.1+ (sem __about__.__version__); o pin honra o teto <4.1
cryptography==49.0.0
itsdangerous==2.2.0

# === Declarative API Bindings ===
jsonpath-ng==1.8.0
jinja2==3.1.6

# === RAG (Onda Q: backend único pgvector) ===
# tiktoken: tokenizer da OpenAI/Azure (cl100k_base) para chunking preciso.
# pgvector: tipo `vector` + operadores de similaridade (<=> cosine, <-> L2,
# <#> inner) + index HNSW/IVFFlat no Postgres. Lib Python registra codec
# asyncpg automático (register_vector).
#
# Onda Q (2026-05-30): qdrant-client REMOVIDO. Qdrant deletado da plataforma.
tiktoken==0.13.0
pgvector==0.5.0

# === Verifier v2 (§14.2 — judge multi-dim + ContractValidator) ===
# jsonschema: validação determinística de output_contract da SKILL.md.
# Opcional em runtime — ContractValidator tem fallback manual se não instalado.
jsonschema==4.26.0


# === RAG ingestão multi-formato (Onda 6) ===
# markitdown: conversor universal (PDF/DOCX/PPTX/XLSX/HTML/MD/CSV/JSON/XML/EPUB/
# Outlook .msg/ZIP/audio/imagens) → markdown limpo. Wrapper unificado de parsers,
# evita 6+ libs separadas.  https://github.com/microsoft/markitdown
# [all] inclui audio-transcription (precisa ffmpeg no sistema) + outras opcionais.
markitdown[all]==0.1.6

# === Tabular Ingestion (Onda Tabular) ===
# DuckDB embarcado para promover CSV/XLSX em tabela consultável via SQL
# parametrizado (bind vars seguras, read-only por execução). Skills consomem
# via seção `## Data Tables` no SKILL.md, paridade declarativa com API
# Bindings. read_csv_auto + read_xlsx nativos: tipos inferidos sem pandas.
# Arquivo .duckdb por tabela em data/tabular/<ks_id>/<table_id>.duckdb.
duckdb==1.5.4

# === Export de apresentação (Cockpit da Conversa → PPTX) ===
# Gera o deck do cockpit (avaliação big-picture + a conversa) server-side, a
# partir do snapshot enviado pela UI. Sem template externo; slides montados no
# código (testável isolado em app/playground/cockpit_pptx.py).
python-pptx==1.0.2

# === Testing (dev) ===
# Testes de lógica pura (Pydantic, parsers, state machines) +
# integration tests com Postgres real (tests/integration/, requer docker).
pytest==9.1.1
pytest-asyncio==1.4.0
# Cobertura de código — reporta % no CI sem bloquear merge.
# Roda com: pytest --cov=app --cov-report=term-missing
# Útil pra ver módulos sem teste (não pra atingir 100% — alvo realista: 70-80%).
pytest-cov==7.1.0
# Pre-commit framework — orquestra hooks locais antes de cada commit.
# Setup: pre-commit install (1x por clone). Config em .pre-commit-config.yaml.
pre-commit==4.6.0
# E2E de interface — Playwright dirige o app REAL num browser headless.
# Testes em tests/e2e/ (marker `e2e`), PULAM se o app não estiver de pé.
# Setup do browser (1x): python -m playwright install chromium
pytest-playwright==0.8.0
